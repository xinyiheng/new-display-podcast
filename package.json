{
  "name": "podcast-display",
  "version": "1.0.0",
  "description": "Beautiful podcast display website with auto-update from GitHub",
  "main": "app.py",
  "scripts": {
    "dev": "python app.py",
    "build": "echo 'No build needed'",
    "start": "python app.py",
    "deploy": "./deploy.sh",
    "zeabur": "gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 app:app",
    "heroku-postbuild": "pip install -r requirements.txt"
  },
  "keywords": ["podcast", "display", "github", "webhook", "flask", "zeabur"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "python": "3.11.x"
  }
}
